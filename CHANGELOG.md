# Changelog

## Unreleased

* Fix binary `predict_proba` for multiple lambda values: preserve the binary logit
  shape so each lambda returns complementary probabilities instead of two ones.

## 0.0.1

* First release on PyPI.
